spring.application.name=my-app

# Default matches docker-compose (root/secret on localhost). Override with SPRING_DATA_MONGODB_URI.
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://root:secret@localhost:27017/daily_agent?authSource=admin}
